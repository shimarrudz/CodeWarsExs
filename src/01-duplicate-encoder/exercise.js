function duplicateEncode(word) {
    word = word.toLowerCase();

    let result = '';
    const charCount = {};

    for (let i = 0; i < word.lenght; i++) {
        const char = word[i];
        charCount[char] = 
    }
}